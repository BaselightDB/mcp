name: Release MCP Bundle

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. 1.0.1 or v1.0.1)'
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '24'

      - name: Install dependencies
        working-directory: mcpb
        run: npm ci

      - name: Stamp release version
        id: version
        working-directory: mcpb
        env:
          INPUT_VERSION: ${{ inputs.version }}
        run: |
          VERSION="${INPUT_VERSION#v}"
          echo "number=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "tag=v${VERSION}" >> "$GITHUB_OUTPUT"
          export VERSION
          echo "Stamping version $VERSION into manifest.json and package.json"
          node -e '
            const fs = require("fs");
            const version = process.env.VERSION;
            for (const file of ["manifest.json", "package.json"]) {
              const json = JSON.parse(fs.readFileSync(file, "utf8"));
              json.version = version;
              fs.writeFileSync(file, JSON.stringify(json, null, 2) + "\n");
            }
          '

      - name: Validate manifest
        working-directory: mcpb
        run: npx @anthropic-ai/mcpb validate manifest.json

      - name: Package bundle
        working-directory: mcpb
        run: npm run pack

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: baselight-${{ steps.version.outputs.number }}
          path: mcpb/baselight.mcpb

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          files: mcpb/baselight.mcpb
          generate_release_notes: true